---
# yaml-language-server: $schema=https://raw.githubusercontent.com/fluxcd-community/flux2-schemas/main/helmrelease-helm-v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app genrpg-dev
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: app-template
  install:
    remediation:
      retries: -1
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  values:
    controllers:
      genrpg-dev:
        strategy: RollingUpdate
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: ghcr.io/iansholtys/genrpg
              tag: 8ae9148
            env:
              TZ: ${TIMEZONE}
              PORT: &port 3000
              COOKIE_SECURE: false
              TRUST_PROXY: loopback
              DATABASE_URL:
                valueFrom:
                  secretKeyRef:
                    name: ${DB_USER}-db-secret
                    key: DATABASE_URL
            envFrom:
              - secretRef:
                  name: genrpg-dev-secret
            probes:
              liveness:
                enabled: true
              readiness:
                enabled: true
            resources:
              requests:
                cpu: 15m
                memory: 64Mi
              limits:
                memory: 256Mi
    service:
      app:
        controller: *app
        ports:
          http:
            port: *port

    persistence:
      config:
        enabled: true
        existingClaim: genrpg-dev-config
        globalMounts:
          - path: /config
      data:
        enabled: true
        existingClaim: genrpg-dev-data
        globalMounts:
          - path: /data
      packages:
        enabled: true
        existingClaim: genrpg-dev-packages
        globalMounts:
          - path: /app/packages
